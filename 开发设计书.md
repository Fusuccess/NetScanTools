# NetScanTools 开发设计书

依据：`README.md`（产品范围与分期）、`NetScanTools_UI_Prototype.md`（界面原型）。  
技术栈已锁定：**Tauri 2 + React + Rust**。  
界面以原型为准；能力与分期以 README 为准。两者冲突时，下文「与原型的差异」一节给出处理方式。

当前仓库尚无应用代码。实现按本文开工。

---

## 1. 目标

做成可打包的 macOS / Windows 图形工具，四个功能页共用同一壳：

1. 局域网扫描（一期）
2. 端口扫描（一期）
3. SSH 本地转发（一期，三期再扩展 `-R` / `-D`）
4. 本机端口映射（四期；一期做页面骨架，功能禁用或标注「四期」）

只扫用户有权限的网络。不捆绑 nmap。不做 Linux 包。不采用 Electron / Flutter。

---

## 2. 总体架构

```
React（src/）                         Rust（src-tauri/）
┌─────────────────────┐              ┌──────────────────────────┐
│ AppShell            │   Tauri IPC  │ lib.rs  注册命令与事件     │
│  顶栏 / 侧栏 / 主题  │ ──────────► │ scanner/  主机+端口探测    │
│  LanScan            │  ◄────────── │ ssh/      本地转发 -L      │
│  PortScan           │   事件推送   │ forward/  端口映射（四期）  │
│  SshTunnels         │              │ nic/      网卡与本机 IP    │
│  PortForward（四期） │              │ store/    配置与隧道列表   │
│  Settings           │              │ oui/      MAC 厂商库       │
└─────────────────────┘              └──────────────────────────┘
```

- **界面只做展示和操作**：发命令、收进度、改本地 UI 状态。
- **Rust 做全部网络 I/O**：探测、SSH、监听转发。长任务可取消。
- **通信**：命令（invoke）发指令；事件（emit）推进度、新主机、SSH 日志、状态变化。

---

## 3. 目录约定

```
src/                          React
  app/
    AppShell.tsx              顶栏、侧栏、主区切换
    theme.ts                  亮/暗主题
  pages/
    LanScan.tsx
    PortScan.tsx
    SshTunnels.tsx
    PortForward.tsx           四期；一期可展示「即将推出」
    Settings.tsx
  components/                 表格、进度条、弹窗、日志
  lib/
    tauri.ts                  封装 invoke / listen
    types.ts                  与 Rust 共用的数据结构（TS 侧）

src-tauri/
  src/
    lib.rs
    nic.rs
    scanner/
      host.rs                 网段展开、ICMP、TCP 探活、ARP、反向 DNS
      port.rs                 TCP 全连接、横幅、取消
    ssh/
      tunnel.rs               russh 本地转发、多隧道启停
    forward/                  四期再实现
    store.rs                  应用数据目录 JSON
    oui.rs                    MAC 前 3 字节 → 厂商
```

一期不建 `forward/` 的真实逻辑，页面占位即可。

---

## 4. 界面对照与交互

原型见 `NetScanTools_UI_Prototype.md`。本节写清每个控件对应的行为，方便按图实现。

### 4.1 应用壳

| 原型元素 | 行为 |
|----------|------|
| 顶栏本机网卡 IP | 启动时拉网卡列表，显示当前选中网卡（默认第一块有 IPv4 的非回环网卡）。切网卡时同步更新。 |
| 顶栏「状态」 | `就绪` / `扫描中` / `隧道运行中`。有扫描任务时优先显示扫描；否则有隧道在跑显示隧道数。 |
| 侧栏四功能 + 设置 + 暗黑模式 | 单页切换，不走浏览器路由。暗黑模式写本地，重启保持。 |
| 设置 | 原型未展开页面。一期只放：默认超时、默认并发、常用端口列表、默认是否勾选 ICMP/TCP。 |

快捷键：局域网扫描页 `Ctrl+R`（macOS 同时支持 `⌘R`）触发开始扫描；扫描中则忽略或改为无效。

### 4.2 局域网扫描

| 原型元素 | 行为 |
|----------|------|
| 网卡下拉 | 列出本机网卡名 + IPv4。切换后网段输入框填该网卡所在 `/24`（可改）。 |
| 网段 | 支持 `192.168.1.0/24` 或 `192.168.1.1-192.168.1.254`。非法则按钮不可点并提示。 |
| 探测参数 | 折叠：超时、并发。默认超时 500ms、并发 64（设置页可改默认值）。 |
| ICMP / TCP 勾选 | 至少勾一项才能开始。TCP 探活端口固定为 22、80、443、445、3389。 |
| 开始 / 取消 | 同时只允许一个扫描任务（局域网或端口，全局一把锁）。 |
| 进度条 | `已完成数 / 地址总数`。 |
| 结果表 | 只展示判定为在线的主机。列：状态、IP、主机名、MAC、厂商、操作。 |
| 展开行 | 延时、ARP 类型、探活时碰到的开放端口及横幅摘要。 |
| 「端口扫描」 | 切到端口扫描页，目标 IP 填入该行 IP，不自动开始。 |

在线规则：ICMP 成功 **或** 任一探活端口 TCP 连通，即视为在线。

拿不到的字段显示 `-`（主机名）或 `Unknown`（厂商），不要编造。

### 4.3 端口扫描

| 原型元素 | 行为 |
|----------|------|
| 目标 IP | 必填，IPv4。可从局域网页跳转带入。 |
| 超时 / 并发 | 默认来自设置。 |
| 预设 | 常用端口（内置列表）/ `1-1024` / `1-65535` / 自定义，四选一。 |
| 自定义 | 支持 `22,80,443,3306,8000-8080`。选「自定义」且为空则不能开始。 |
| 结果表 | 端口、协议（一期只有 TCP）、状态（OPEN / CLOSED / TIMEOUT）、Known Service、Banner。 |
| 汇总 | 开放 / 关闭 / 超时计数，扫描中实时更新。 |

Known Service：本地端口号对照表（22→SSH、80→HTTP…），不是指纹识别。  
Banner：连通后读前若干字节（限时），HTTP 再解析 `Server` 或状态行；失败则 `-`。

`1-65535` 必须可取消，并有并发上限保护（设置里 concurrent 最大值建议 256）。

### 4.4 SSH 隧道

列表为主，弹窗新建/编辑。一期只做 **本地转发 `-L`**。

| 原型元素 | 行为 |
|----------|------|
| 卡片开关 | ON：启动该隧道；OFF：停止。启动失败开关弹回 OFF，日志写原因。 |
| 路径展示 | `本机 127.0.0.1:{local_port} → [跳板 user@host:ssh_port] → {target_host}:{target_port}` |
| 编辑 / 删除 | 运行中不可删；先停再删。编辑运行中的隧道：保存后需重启才生效（提示用户）。 |
| 查看日志 | 底部日志区切换到该隧道的日志流。 |
| 弹窗认证 | 密码或私钥二选一。私钥用文件选择器，存的是路径不是密钥内容。 |
| 本地监听 | 原型只填端口。一期固定绑 `127.0.0.1`（避免一开隧道就对外暴露）。 |

按钮补全（原型只有「保存并启动」）：弹窗提供 **保存** 与 **保存并启动**，否则无法留下「已停止」的配置。

一期可同时运行多条隧道（与原型列表一致）。README 里「一期一条」按原型放宽为：一期就做多条启停；二期再补钥匙串与断线重连。

密码：**一期不落盘**。保存配置时密码留空；下次启动若是密码认证，弹窗或内联输入再连。私钥路径可落盘。二期改存系统钥匙串。

主机密钥：一期 Trust-On-First-Use，指纹写入应用数据目录；再次连接指纹变化则失败并写日志，不静默覆盖。

### 4.5 本机端口映射（四期）

一期：侧栏可进页面，表格按原型画静态空态 + 文案「四期实现」。  
四期：入口绑网卡 IP（含 `0.0.0.0` / `127.0.0.1`）、入口端口 → 目标 IP:端口；连接数；启停。TCP 字节转发，不经 SSH。

---

## 5. 数据模型

Rust `serde` 与前端 `types.ts` 字段同名、snake_case。

```text
Nic {
  name, ipv4, cidr_hint        // cidr_hint 如 192.168.1.0/24
}

LanScanRequest {
  nic_name,
  range,                       // "192.168.1.0/24" 或 "a-b"
  icmp, tcp_probe,
  timeout_ms, concurrency
}

HostRow {
  ip, hostname,                // hostname 可空
  mac, vendor,                 // 跨网段 mac/vendor 为空
  rtt_ms,                      // 可空
  arp_kind,                    // "dynamic" | 空
  probe_ports: [{ port, banner }]
}

PortScanRequest {
  ip, ports,                   // 展开后的端口列表由前端解析或后端解析；建议后端解析字符串
  timeout_ms, concurrency
}

PortRow {
  port, proto,                 // proto 恒为 "tcp"
  state,                       // "open" | "closed" | "timeout"
  service, banner
}

SshTunnelConfig {
  id, alias,
  local_bind,                  // 一期恒 127.0.0.1
  local_port,
  jump_host, jump_port, username,
  auth: "password" | "key",
  key_path,                    // auth=key 时
  target_host, target_port
}

SshTunnelView {
  ...SshTunnelConfig,
  status: "running" | "stopped" | "error",
  uptime_secs, last_error
}

Settings {
  timeout_ms, concurrency,
  common_ports,                // 数字数组
  default_icmp, default_tcp,
  theme: "light" | "dark"
}
```

---

## 6. IPC 一览

命令（前端 invoke）：

| 命令 | 入参 | 返回 |
|------|------|------|
| `list_nics` | — | `Nic[]` |
| `get_settings` / `save_settings` | `Settings` | `Settings` |
| `start_lan_scan` | `LanScanRequest` | `{ task_id }` |
| `start_port_scan` | `PortScanRequest` | `{ task_id }` |
| `cancel_scan` | `{ task_id }` | — |
| `list_tunnels` | — | `SshTunnelView[]` |
| `save_tunnel` | `SshTunnelConfig` + 可选一次性 `password` | `SshTunnelView` |
| `start_tunnel` | `{ id, password? }` | `SshTunnelView` |
| `stop_tunnel` | `{ id }` | — |
| `delete_tunnel` | `{ id }` | — |
| `set_theme` | `{ theme }` | — |

事件（Rust emit → 前端 listen）：

| 事件 | 载荷 |
|------|------|
| `scan:progress` | `{ task_id, done, total }` |
| `scan:host` | `{ task_id, host: HostRow }` |
| `scan:port` | `{ task_id, row: PortRow }` |
| `scan:finished` | `{ task_id, error? }` |
| `ssh:status` | `SshTunnelView` |
| `ssh:log` | `{ id, ts, level, message }` |

同一时刻只允许一个 `start_*_scan`。已有扫描再点开始：提示先取消或直接拒绝。SSH 与扫描互不阻塞。

---

## 7. 本机实现要点

### 7.1 网段与扫描

1. 解析 CIDR / 起止 IP，去掉网络地址、广播地址（`/31` `/32` 除外）。
2. 并发池：`concurrency` 个 worker，每个地址：可选 ICMP，再可选一组 TCP connect（探活端口并行或顺序，超时取 `timeout_ms`）。
3. ICMP：各平台用非原始套接字方案（macOS 可用系统 ping 能力或 ICMP echo API；Windows 用 `IcmpSendEcho`）。失败不算离线，还要看 TCP。
4. 在线后再做：反向 DNS（短超时）、读本机 ARP/邻居表匹配 MAC、OUI 查厂商。
5. 取消：任务令牌；worker 在下一地址前退出；已发出的 connect 随超时结束。

不扫 UDP。不做半开扫描。不做操作系统指纹。

### 7.2 端口扫描

TCP `connect` + 超时。连通 → OPEN 并尝试读 banner；连接拒绝 → CLOSED；到期 → TIMEOUT。  
自定义端口串在 Rust 侧解析，非法片段报错、不开始。

### 7.3 SSH

用 Rust SSH 库（如 `russh`）实现 `-NfL` 语义：认证成功后在 `127.0.0.1:local_port` listen，每个入站连接经 SSH 直连 `target_host:target_port`。  
本地端口占用：启动失败，日志写 `address already in use`。  
不调用系统 `ssh` 二进制。

### 7.4 存储

应用数据目录（Tauri `app_data_dir`）：

- `settings.json`
- `tunnels.json`（无密码）
- `known_hosts.json`
- `theme` 可放进 `settings.json`

厂商库：编译期嵌入一份精简 OUI 映射（常见前缀即可，未知为 Unknown）。

---

## 8. 状态与错误

| 场景 | 表现 |
|------|------|
| 网段非法 / IP 非法 | 按钮禁用或点击后顶栏/页内错误，不发命令 |
| 扫描中再扫 | 提示「请先取消当前扫描」 |
| 扫描失败（权限、无网卡） | `scan:finished.error`，进度停止，状态回就绪 |
| SSH 认证失败 | 开关回 OFF，日志 ERROR |
| SSH 指纹变化 | 失败，提示「主机密钥不匹配」 |
| 监听端口占用 | 失败，写明日志 |
| 四期功能 | 按钮不可用 |

日志级别：INFO / WARN / ERROR。SSH 日志按隧道 id 分桶，界面只显示当前选中隧道。

---

## 9. 分期对照

| 能力 | 一期 | 二期 | 三期 | 四期 |
|------|------|------|------|------|
| Tauri 壳、侧栏、主题、设置默认值 | ✓ | | | |
| 局域网扫描表 + 展开详情 + 跳转端口扫描 | ✓ | | | |
| 端口扫描预设/自定义 + banner | ✓ | | | |
| SSH `-L` 多条、密码/私钥、日志 | ✓ | | | |
| mDNS / UPnP / SNMP 补名称型号 | | ✓ | | |
| 扫描历史导出 | | ✓ | | |
| 密码进钥匙串、SSH 断线重连 | | ✓ | | |
| SSH `-R` / `-D` | | | ✓ | |
| 可选系统 nmap 增强 | | | ✓ | |
| 本机端口映射真功能 | | | | ✓ |

---

## 10. 与原型 / README 的差异

| 点 | 处理 |
|----|------|
| 原型有设置、暗黑模式，README 未写 | 按原型做。设置一期只做扫描默认值。 |
| README 一期「一条 SSH」，原型是列表多条 | 按原型做多条。 |
| 原型 SSH 弹窗只有「保存并启动」 | 增加「保存」，以支持已停止配置。 |
| 原型 SSH 未选监听网卡 | 一期固定 `127.0.0.1`。 |
| 原型设置页无稿 | 见 4.1，不扩展成通用配置中心。 |
| 端口映射在原型里画了完整表 | 一期占位，四期接 `forward/`。 |
| README 二期才有设备名增强 | 一期表头仍留「主机名」，值来自 DNS/ARP；空就显示 `-`。 |

---

## 11. 一期实现顺序

1. 初始化 Tauri 2 + React，搭 AppShell（网卡、状态、侧栏、主题）。
2. `list_nics` + 设置读写。
3. 局域网扫描：解析网段 → 探活 → 表 + 进度 + 取消。
4. ARP/DNS/OUI 填充；展开行；跳转端口扫描。
5. 端口扫描全流程。
6. SSH 配置落盘 + 列表；再接通启动/停止/日志。
7. 端口映射页占位。
8. macOS / Windows 各打一次开发包，确认网卡列表和一次小网段扫描能跑。

---

## 12. 一期验收

- 能选出网卡，网段自动带出，可改 CIDR 后扫描，可取消。
- 在线主机出现在表格；同网段尽量有 MAC/厂商；「端口扫描」能带 IP 跳转。
- 端口扫描三种预设 + 自定义范围可用；OPEN 行尽量有 banner。
- SSH：保存两条配置，一条密码（当次输入）、一条私钥；能转到目标端口；日志可见；停止后本地端口释放。
- 暗黑模式切换有效。
- 未实现四期映射真功能，页面有明确说明。

不验收：设备型号、UDP、半开扫描、nmap、`-R`/`-D`、端口映射转发、Linux。
